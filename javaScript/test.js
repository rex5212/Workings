const { log } = require("console")

// x = 0
// x = 20
// x = 40

// const xx = {'teste1':10, 'teste2':12, 'teste3':13}
// let texto = ""

// 0,0,1
// 0,0,20

// let z = {
    
// }

// z = Object.fromEntries(
//     Object.entries(z).filter(([field, value]) => {
//         if (value === "" || value === null || value === undefined) return false;
//         if (Array.isArray(value)) return value.some(v => v !== "" && v !== null && v !== undefined);
//         return !(typeof value === "object" && !Array.isArray(value) && value !== null &&
//             Object.values(value).every(v => v === "" || v === null || v === undefined || (Array.isArray(v) && v.length === 0)));
//     })
// );
// console.log(z)

// let obtest = {}

// console.log(Object.keys(obtest).length)
// console.log(typeof true)

// const datas = [null, false, undefined, NaN, 0, ""];

// datas.forEach(data => {
//     if (data) {
//       console.log(`Will run: ${data}`);
//     } else {
//       console.log(`Will NOT run: ${data}`);
//     }
// })

// function xtest(name) {
//     console.log("função xtest")
// }

// xtest()
// let value = 12500.5;
// let formatted = value.toLocaleString("pt-BR", {
//   minimumFractionDigits: 2,
//   maximumFractionDigits: 2
// });

// console.log(formatted); // "12.500,50"

// let xy = 12500
// console.log(xy.toLocaleString("pt-BR", {
//     useGrouping: false,
//     minimumFractionDigits: 2,
// }))

// let array = [1,2,3]
// console.log(4 - (array.length%4 > 0 ? array.length%4 : 4))
